# Write a program in python that converts a string into a list of its characters

def string_to_list():
    str1 = input("enter the string: ")
    list1 = (list(str1))
    print("the string {",str1,"} converted to list of character is:",list1)
    return 
string_to_list()
